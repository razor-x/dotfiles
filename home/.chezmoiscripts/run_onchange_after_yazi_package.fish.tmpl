#!/usr/bin/env fish

# yazi package hash: {{ include (joinPath .chezmoi.sourceDir "dot_config" "yazi" "package.toml") | sha256sum }}

if type --query ya; and type --query yazi
    ya pkg install
else
    echo 'Skipping Yazi package install: yazi or ya not installed.'
end
