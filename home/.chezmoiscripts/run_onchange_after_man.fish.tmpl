#!/usr/bin/env fish

# hash: {{ include (joinPath .chezmoi.sourceDir "dot_local" "share" "man" "man7" "dotfiles.7") | sha256sum }}

if type --query mandb
    mandb --user-db
else
    echo 'Cannot update man database: mandb not installed.'
    return 1
end
