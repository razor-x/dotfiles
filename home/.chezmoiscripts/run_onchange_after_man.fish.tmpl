#!/usr/bin/env fish

# man hash: {{ include (joinPath .chezmoi.sourceDir "dot_local" "share" "man" "man7" "dotfiles.7") | sha256sum }}

if type -q mandb
    mandb
else
    echo "Skipping mandb update: mandb not installed."
end
