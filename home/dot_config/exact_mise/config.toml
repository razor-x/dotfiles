[settings]
# UPSTREAM: color_theme is not released yet.
# color_theme = '{{ template "theme" (merge (dict "app" "mise") .) }}'

idiomatic_version_file_enable_tools = [
  'dotnet',
  'node',
  'npm',
  'pnpm',
  'python',
  'ruby',
  'yarn'
]

ruby.compile=false
